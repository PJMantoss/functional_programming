//Combine 2 arrays using the concat method

function nonMutatingConcat(original, attach) {
  // Add your code below this line
    return original.concat(attach);

  // Add your code above this line
}
let first = [1, 2, 3];
let second = [4, 5];
nonMutatingConcat(first, second);
